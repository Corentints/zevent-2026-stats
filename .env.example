# URL publique du worker Cloudflare (voir worker/) qui sert l'historique
# partagé des dons. Laisser vide pour retomber sur l'accumulation locale
# (par navigateur, via localStorage).
# Exemple : https://zevent-history-worker.<ton-compte>.workers.dev
VITE_HISTORY_API_URL=
